# Local admin frontend override.
# Leave empty for local dev when the Vite proxy forwards /api to http://localhost:8787.
# Set to your stable Worker API origin for production builds.

VITE_LINKETRY_API_URL=
